<!doctype html>
<html>
  <head>
    <title>Owls!</title>
  </head>
  <body>
    Name: <%= owl[0].name %> Age: <%= owl[0].age %>
    <a href='/owls/edit/<%= owl[0]._id %>'>Edit</a>
    <form action='/owls/destroy/<%= owl[0]._id %>' method='POST'>
      <input type='submit' value='Delete Owl'>
    </form>
  </body>
</html>
